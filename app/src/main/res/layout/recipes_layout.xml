<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/desserts_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableEnd="@drawable/ic_action_desserts"
        android:text="Desserts"
        android:textAlignment="viewStart" />

    <Button
        android:id="@+id/meat_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableEnd="@drawable/ic_action_meat"
        android:text="Meat"
        android:textAlignment="viewStart" />

    <Button
        android:id="@+id/pasta_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableEnd="@drawable/ic_action_pasta"
        android:text="Pasta"
        android:textAlignment="viewStart" />

    <Button
        android:id="@+id/salad_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableEnd="@drawable/ic_action_salad"
        android:text="Salad"
        android:textAlignment="viewStart" />

    <Button
        android:id="@+id/soup_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableEnd="@drawable/ic_action_soup"
        android:text="Soup"
        android:textAlignment="viewStart" />

    <Button
        android:id="@+id/vegetable_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableEnd="@drawable/ic_action_vegetable"
        android:text="Vegetables"
        android:textAlignment="viewStart" />
</LinearLayout>